[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "Painting_app"
version = "0.1.0"
description = "A simple Tkinter painting application"
requires-python = ">=3.8"

[project.scripts]
painting = "painting.app:main"
